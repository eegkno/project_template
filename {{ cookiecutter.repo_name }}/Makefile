# Makefile

# This generates a symbolic link (shortcut) where all the data is shared.
datalink:
	echo "To be defined"

# Install python librareis for devlopment environment
Pydev-install:
	pip install -r requirements-dev.txt

# Install python libraries for production environment
Pyprod-install:
	pip install -r requirements.txt

# Install R libraries
Rinstall:
	echo "To be defined"

# Any other Linux libraries to be installed
install:
	echo "To be defined"
